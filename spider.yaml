# Generated by Spider

# Generates unit tests to verify that the assets exists in assets directory
generate_tests: false

# Use this to remove vcs noise created by the `generated` comments in dart code
no_comments: true

# Exports all the generated file as the one library
export: true

# This allows you to import all the generated references with 1 single import!
use_part_of: true

# Location where all the generated references will be stored
package: resources

groups:
  - path: assets/bottom_nav_icon
    class_name: BottomNavIcon
    types: [ .png, .jpg, .jpeg, .webp, .webm, .bmp ]
  - path: assets/images/common
    class_name: CommonImages
    types: [ .png, .jpg, .jpeg, .webp, .webm, .bmp ]
  - path: assets/images/onboarding
    class_name: OnboardingImages
    types: [ .png, .jpg, .jpeg, .webp, .webm, .bmp ]
  - path: assets/images/login
    class_name: LoginImages
    types: [ .png, .jpg, .jpeg, .webp, .webm, .bmp ]
  - path: assets/images/home_screen
    class_name: HomeScreenImage
    types: [ .png, .jpg, .jpeg, .webp, .webm, .bmp ]
  - path: assets/icons/
    class_name: AppIcons
    types: [ .png, .jpg, .jpeg, .webp, .webm, .bmp ]
#  - path: assets/vectors
#    class_name: Svgs
#    package: res
#    types: [ .svg ]